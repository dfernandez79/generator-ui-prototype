language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ui-prototype' ]; then cd .. && eval "mv $currentfolder generator-ui-prototype" && cd generator-ui-prototype; fi

